---
title: Halftone Video Hero
description: A hyper-optimized dual-RAF Canvas component mapping live video into highly detailed, physics-based vertical halftone strings that react to the cursor.
component: true
badge: new
category:
  - image-effects
author: Satisium UI
links:
  github: https://github.com/satisium-ui/ui/blob/master/content/docs/components/halftone-video-hero.mdx
registryKeys:
  - halftone-video-hero-demo
media:
  image: ""
  video: "https://res.cloudinary.com/ddon6aux0/video/upload/v1782129926/ui-v3/demos/videos/1.mp4"
---

## Install

### CLI

<div className="mt-6">
  <CommandBlock cli="halftone-video-hero" title="halftone-video-hero" />
</div>

### Manual

**1. Add Source Code**

import { halftoneVideoHeroFile } from "@/registry/strings/halftone-video-hero"

<div className="mt-6">
  <CodeBlock files={halftoneVideoHeroFile} height="600px" />
</div>

_Note: This component utilizes pure Canvas API via a highly optimized double RequestAnimationFrame loop. No WebGL or Three.js dependencies are required._

## Props

| Prop                   | Type              | Default     | Description                                                          |
| :--------------------- | :---------------- | :---------- | :------------------------------------------------------------------- |
| **`videoUrl`**         | `string`          | —           | The source URL of the video stream.                                  |
| **`lines`**            | `number`          | `140`       | Number of vertical halftone lines. Higher = more detail but heavier. |
| **`pointsPerLine`**    | `number`          | `250`       | Vertical resolution points per line. Higher = smoother wave curves.  |
| **`maxLineThickness`** | `number`          | `0.85`      | Max thickness multiplier of the lines relative to their spacing.     |
| **`contrast`**         | `number`          | `1.4`       | Contrast curve for the thickness. Higher = darker shadows.           |
| **`hoverRadius`**      | `number`          | `120`       | Radius of the mouse interaction in pixels.                           |
| **`mouseForce`**       | `number`          | `0.15`      | Force applied by the mouse to push the lines.                        |
| **`stiffness`**        | `number`          | `0.015`     | Spring stiffness (tendency to return to origin).                     |
| **`friction`**         | `number`          | `0.92`      | Velocity damping/friction (how quickly it settles).                  |
| **`tension`**          | `number`          | `0.25`      | Tension between vertical points on the same line.                    |
| **`fallback`**         | `React.ReactNode` | `null`      | Optional fallback UI rendered via Suspense while media loads.        |
| **`className`**        | `string`          | `undefined` | Standard React classNames for the container wrapper.                 |
