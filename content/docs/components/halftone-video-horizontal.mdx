---
title: Halftone Video Horizontal
description: A hyper-optimized dual-RAF Canvas component mapping live video into highly detailed, physics-based horizontal halftone strings that react to the cursor's Y-velocity.
component: true
badge: new
category:
  - image-effects
author: Satisium UI
links:
  github: https://github.com/satisium-ui/ui/blob/master/content/docs/components/halftone-video-horizontal.mdx
registryKeys:
  - halftone-video-horizontal-demo
media:
  image: ""
  video: "https://res.cloudinary.com/ddon6aux0/video/upload/v1782129926/ui-v3/demos/videos/1.mp4"
---

## Install

### CLI

<div className="mt-6">
  <CommandBlock
    cli="halftone-video-horizontal"
    title="halftone-video-horizontal"
  />
</div>

### Manual

**1. Add Source Code**

import { halftoneVideoHorizontalFile } from "@/registry/strings/halftone-video-horizontal"

<div className="mt-6">
  <CodeBlock files={halftoneVideoHorizontalFile} height="600px" />
</div>

_Note: This component utilizes pure Canvas API via a highly optimized double RequestAnimationFrame loop. No WebGL or Three.js dependencies are required._

## Props

| Prop                   | Type              | Default     | Description                                                               |
| :--------------------- | :---------------- | :---------- | :------------------------------------------------------------------------ |
| **`videoUrl`**         | `string`          | —           | The source URL of the video stream.                                       |
| **`lines`**            | `number`          | `80`        | Number of horizontal rows. Higher = more vertical detail.                 |
| **`pointsPerLine`**    | `number`          | `160`       | Resolution points across each line. Higher = smoother curves.             |
| **`maxLineThickness`** | `number`          | `0.85`      | Max thickness multiplier of the lines relative to their vertical spacing. |
| **`contrast`**         | `number`          | `1.4`       | Contrast curve for the thickness. Higher = darker shadows.                |
| **`hoverRadius`**      | `number`          | `120`       | Radius of the mouse interaction in pixels.                                |
| **`mouseForce`**       | `number`          | `0.15`      | Force applied by the mouse Y-velocity to push the strings.                |
| **`stiffness`**        | `number`          | `0.015`     | Spring stiffness (tendency to return to horizontal origin).               |
| **`friction`**         | `number`          | `0.92`      | Velocity damping/friction (how quickly it settles).                       |
| **`tension`**          | `number`          | `0.25`      | Tension between horizontal points on the same string.                     |
| **`fallback`**         | `React.ReactNode` | `null`      | Optional fallback UI rendered via Suspense while video buffers.           |
| **`className`**        | `string`          | `undefined` | Standard React classNames for the container wrapper.                      |
