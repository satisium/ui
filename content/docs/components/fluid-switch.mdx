---
title: Fluid Switch
description: A highly tactile, spring-physics driven toggle switch that feels deeply physical.
component: true
badge: new
stack: ["motion/react", "tailwind"]
links:
  github: https://github.com/your-org/satis-ui/blob/main/registry/ui/fluid-switch.tsx
  preview: https://github.com/your-org/satis-ui/blob/main/registry/ui/fluid-switch.tsx
registryKeys: ["fluid-switch-demo", "fluid-switch-labeled"]
---

## Overview

The Fluid Switch is designed to replace the standard HTML checkbox. We use `motion/react` to apply spring physics to the thumb, giving it a subtle squish effect when tapped, mimicking the tactile feedback of a physical hardware toggle.

## Installation

Run the following command to install the component into your project. Note that it requires `motion` to be installed.

```bash
npx shadcn@latest add https://satis-ui.com/r/fluid-switch.json
```

<div className="mt-6">
  <CommandBlock cli="https://satis-ui.com/r/button.json" />
</div>

import { fluidSwitchFile } from "@/registry/strings/fluid-switch"

<div className="mt-6">
  <CodeBlock files={fluidSwitchFile} height="600px" />
</div>
